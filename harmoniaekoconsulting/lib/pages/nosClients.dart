import 'package:flutter/material.dart';

class NosClients extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Nos Clients'),
      ),
      body: Center(
        child: Text('Nos Clients Page'),
      ),
    );
  }
}
